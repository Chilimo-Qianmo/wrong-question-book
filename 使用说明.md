# 学生错题集生成器 v2

选择「试卷 PDF」与「答题情况 Excel」，自动识别**选择题**题干与选项，为每位学生生成
『错题集_学生姓名.docx』并归档到 `错题集/[班级]/[日期]-[试卷名]/`。

v2 是完整重写：**双通道 PDF 解析引擎 + Vue3 界面**，修复了旧版一批会产出错误内容的缺陷。

---

## 一、快速开始

**方式一：双击 exe（推荐给老师）**
双击 `dist\错题集生成器.exe`。程序会启动本地服务并自动打开界面（浏览器或原生窗口）。
`题目配置.json`、`图片` 文件夹与 exe 放同一目录即可，程序会自动创建
`错题集`、`题目配置`、`图片` 子文件夹。

**方式二：双击 `启动.bat`（需 Python）**
首次会自动安装依赖（需要联网）；若 `web/dist` 不存在且本机有 Node.js，会自动构建界面。

**方式三：手动启动**
```
pip install -r requirements.txt
python -m app.launcher                # 自动打开界面
python -m app.launcher --no-window    # 只起服务
```

---

## 二、界面流程（左侧六步）

| 步骤 | 做什么 |
|---|---|
| ① 选择来源 | 选试卷 PDF 或题目配置 JSON（二选一）；**选完 PDF 立刻做「来源体检」**，显示判定为电子版/扫描版/混合、逐页文字层字符数、表格与图片数 |
| ② 题目核对 | 逐题显示题干与选项，标注**来源**（文字层/OCR/配置/混合）与**置信度**；缺口、告警、被忽略的文字全部可见；每题勾选「已核对」后才能生成 |
| ③ 题目配图 | 「从 PDF 自动抽取配图」可把电子版试卷的插图自动归属到题并导出；也可手工添加/删除 |
| ④ 生成 | 实时进度 + 运行日志；完成后显示归档路径并可一键打开文件夹 |
| ⑤ 合并 | 选择多个错题集文件夹，按班级自动分组合并（同名学生会合并为一份，编号自动续接） |
| ⑥ 设置 | 输出目录、图片根目录、班级、标题、版式常量（字体/字号/行距/页边距） |

---

## 三、v2 相对旧版的关键改进

### 1. 电子版试卷不再走 OCR（速度与准确度质变）
自动探测 PDF 文字层：
- **电子版**（有文字层）：用 pdfplumber 直接解析文字、**结构化表格**、**内嵌插图**，
  识别 16 道题耗时从约 60 秒降到 **1 秒以内**，且没有 OCR 错字。
- **扫描版**（整页图片）：渲染后 OCR，并用 OpenCV 线检测识别表格框线。
- **混合卷**：按页分别走两条通道。

### 2. 表格自动识别
电子版直接拿到单元格结构；扫描版用 OpenCV 找框线（旧版靠 OCR 文本框聚列，
实测会漏检）。答不出选项的题（选项就是表格）自动按「答案表格」渲染成 Word 表格。

### 3. 配图自动抽取
电子版试卷的插图带精确坐标，可按题目纵向区间**自动归属并导出**到 `图片/<题号>/`，
旧版只能手工截图。测试卷 6 张图 6/6 归属正确。

### 4. 修复的识别缺陷（均有实测复现）
| 旧版问题 | v2 |
|---|---|
| 题号与题干同一行时按 y 排序错位，题干/选项内容跑到上一题 | 改为「行聚类」，纵向重叠 >50% 即同一行 |
| 中间某题题号漏识别 → 后续整段被吞并成一题、其余题全丢 | 按检测到的题号开新题，并产出**缺口告警**，界面横幅提示 |
| OCR 缓存不与试卷绑定，换卷后直接套用上一份卷子的识别结果 | 缓存键 = 路径+大小+修改时间+页数+**代码签名**，换卷/改代码立即失效 |
| 题目配置缺少 options 时，快路径跳过 OCR，导致所有选项丢失 | 配置完整性校验 + 逐题覆盖（配置优先、缺项回退 OCR） |
| 配置查找按文件名子串匹配、且永远命中最早那份 | 精确匹配 + 按日期取最新 |
| 图注/坐标轴文字混进题干 | 电子版按图表区域排除；扫描版按正文栏过滤 |
| 识别错的文本被静默丢弃 | 全部记入「已忽略文字」，界面可查看、可一键还原 |

---

## 四、目录结构

```
app/                 后端（FastAPI + 引擎）
  core/pdf_router.py   文字层探测与通道路由
  core/digital.py      电子版解析（pdfplumber）
  core/scanned.py      扫描版解析（渲染 + OCR + OpenCV）
  core/lines.py        行聚类
  core/recognize.py    题号/题干/选项切分与质量评估
  core/tables.py       表格线检测与单元格
  core/figures.py      配图抽取与归属
  core/docx_build.py   Word 生成
  core/archive.py      归档与清理
  core/merge_docx.py   多重合并
  core/excel.py        答题表解析
  core/cache.py        指纹化缓存
web/                 前端（Vue3 + Vite，构建产物在 web/dist）
tests/regression.py  回归测试（两份真实试卷的快照对比）
legacy/              旧版 tkinter 界面与旧核心（保留一个版本周期）
docs/PLAN.md         升级方案    docs/API.md  前后端接口契约
```

---

## 五、常见问题

**Q：需要联网吗？**
运行不需要。首次安装依赖或构建前端需要联网。

**Q：识别结果不理想怎么办？**
核对页每题都标了来源与置信度：置信度低或有告警的题会高亮，直接改文本即可；
顶部若提示「第 X 题未识别到」，说明该题题号没被识别出来，可在核对页确认后手工补。

**Q：扫描件识别不准？**
扫描件依赖 OCR，边缘的竖排「准考证号/姓名」等文字有时会被 OCR 并进正文。
建议提供电子版 PDF；若只有扫描件，请在核对页重点检查有告警的题目。

**Q：想改 Word 版式？**
界面「⑥ 设置」里可改字体、字号、行距、页边距；更细的版式在
`app/core/docx_build.py` 顶部的 `LAYOUT`。

**Q：旧版还能用吗？**
能，代码在 `legacy/`，见 `legacy/README.md`。

**Q：重新打包或替换 exe 时提示「文件被占用 / 拒绝访问」？**
单文件打包的程序运行时会产生一个子进程。若上一次的窗口是被强杀的，子进程可能仍在后台运行并占用 exe。
到任务管理器结束残留的「错题集生成器」进程后再替换即可。

**Q：升级到新版本后需要手动清理缓存吗？**
不需要。版本号已参与缓存签名（打包环境用版本号、源码环境用代码指纹），换版本会自动失效旧缓存。
想手动清理可删除程序目录下的 `.cache` 文件夹（只是版面缓存，删掉会重新解析一次）。

**Q：回归测试怎么跑？**
```
python tests/regression.py            # 对比快照
python tests/regression.py --update   # 用当前结果重写快照
python tests/test_docx_format.py      # Word 版式符合性（16 项）
```

---

## 六、更新记录

### 2026-09 · 版式与易用性更新

| 项目 | 说明 |
|---|---|
| **Word 版式** | 页边距上下左右 1.27cm、页眉学生姓名、页眉距/页脚距 0.8cm、页脚居中页码（1、2、3）；题干不缩进、选项缩进 2 字符、宋体五号、行距 1.1、段前段后 0。已用 `tests/test_docx_format.py` 逐条锁定（16/16 通过） |
| **配图尺寸** | 插图按**原图尺寸**插入（用图片 DPI 还原物理尺寸），只有超过页面宽度/高度上限时才等比缩小；不再一律拉成 6 英寸宽 |
| **核对页两栏** | 左侧 2/3 是原有全部功能（识别概览 + 逐题编辑 + 勾选），右侧 1/3 实时显示**当前题在原始试卷上的区域**，可缩放、适应宽度、在新窗口打开；窗口较窄时自动改为上下排列 |
| **配图页可视化** | 改为**题目缩略图墙**：一眼看出每题有没有图、有几张、长什么样；点选题目后显示大缩略图，支持大图预览、添加/删除、一键从 PDF 自动抽取 |
| **文件对话框** | 声明进程 DPI 感知（Per-Monitor V2）并按主屏 DPI 缩放 Tk，修复 4K / 150% 缩放下系统「选择文件」对话框过小、模糊的问题 |

### 2026-09 · 反馈问题修复

| 问题 | 处理 |
|---|---|
| **表格会重复插入** | ① 过滤误检表格——PDF 里的「答题卡编号条」（一排 1 2 3 4…）以前被当成 8 列表格插进题目，现在识别为误检并放进「已忽略」；② 表格自动去掉首尾空行空列；③ 与表格重叠超过 60% 的配图不再重复插入 |
| **题干首行缩进** | 题干改为**首行缩进 2 字符**（与选项一致），版式测试同步更新（16/16 通过） |
| **文档标题** | 标题只保留 **「错题集」** 三个字，去掉了「姓名 错题集」与副标题「试卷名 · 选择题错题整理」；**页眉仍然是学生姓名**，导出的文件名也仍是 `错题集_姓名.docx` |
| **图片偏大** | 配图最大宽度改为「**正文宽度的 60%**」（约 4.36 英寸），超过才等比缩小；无 DPI 信息的截图按 144 DPI 估算（原来按 96 会算得偏大）。比例可在「⑥ 设置」里调整 |
| **核对页原题区域** | 改为**每道题一行**：左侧 2/3 是题目编辑卡片，右侧 1/3 紧挨着显示**这道题自己的原题区域**，两边自动对齐；图片宽度 100% 自适应窗口，不再需要点一道看一道 |
| **核对页配图不显示** | 修复：识别到的配图以前只有坐标没有图片地址，前端显示为破图；现在直接用原卷区域裁剪地址显示 |
| **配图导入方式** | 图片**平铺**放在「图片」目录，**不再需要新建 1、2、3 子文件夹**；「③ 题目配图」左边是图片素材库、右边是题目格，**把图片拖到题目上**即可配图（也支持点选图片再点题目）；「从 PDF 自动抽取配图」保留；旧的 `图片/<题号>/` 目录首次打开会自动迁移到平铺结构 |

### v2.2

| 项目 | 说明 |
|---|---|
| **选项完整显示** | 核对页的选项由单行输入框改为**自动换行的文本域**，长选项一次看全，不再被截断（文本域高度随内容自动增长） |
| **1:1 分栏** | 核对页由「2:1」改为**左右各半**，题目编辑区与原题区域等宽 |
| **放大改为弹层** | 原题区域的「放大」按钮改为**弹层看图**（与配图页看图一致的体验），支持新窗口打开 |
| **悬停放大镜** | 鼠标移到原题区域图片上时，**图片下方弹出小窗**显示指针附近的放大画面（260%），移开即消失，方便看清小字 |
| **忽略列表默认收起** | 「已忽略的疑似图/表文字」**默认收起**，需要时点标题展开（以前有问题就自动展开，信息量太大） |
| **配图预览修复** | 修复旧配置/旧流程存下的配图没有图片地址、核对页显示「配图暂不可预览」的问题：现在会自动补上按区域裁剪的地址；确实无法显示时进「已忽略」并说明原因 |
| **图片库默认无图片** | 修正默认设置（此前被测试写坏了），图片库默认只读取你配置的「图片」目录，为空就是空的；另新增**「清空图片库」**按钮可一键清空图片与配图关系 |

### v2.3

| 项目 | 说明 |
|---|---|
| **增删题目** | 核对页每道题左上角可直接**改题号**，右上角「删除本题」可移除；底部新增**「＋ 添加题目」**，没有识别结果时也能手动添加自编题目（来源标为「自编」） |
| **去掉小绿点** | 左侧步骤导航不再显示状态小圆点 |
| **确认核验 → 配图** | 右下角按钮由「确认核验并生成」改为**「确认核验」**，点击后进入**③ 题目配图**，不再直接开始生成；生成改由④页「开始生成」触发。配图页底部新增「下一步：生成 →」 |
| **配图不再重复累积** | 修复「识别 → 生成 → 再识别」时同一张配图被反复累加的问题：生成时不再把自动检测到的配图写进题目配置，且应用配置时会按页面坐标去重 |
| **扫描件不再丢题** | 修复「贵阳卷第 3 题消失」：左侧竖排的「姓名/准考证号」会被 OCR 并进正文行并排在题号前面，导致题号被判为「不在行首」。现在识别题号时会跳过这类边栏字，并新增**兜底切分**：题号若被并进别的文本框中间，会自动切开恢复该题。该卷实测由 15 题恢复到 **16 题**，第 1 题选项也从 2 个恢复为 4 个 |
| **全新环境** | 打包后的 exe 首次启动会自动在**自己所在目录**创建 `错题集`、`图片`、`题目配置` 三个文件夹，并生成默认 `settings.json`，换台电脑/换个目录打开即是可用状态 |
| **本地测试目录** | 去掉了测试用的 `WTT_SETTINGS` 环境变量；`.gitignore` 增加 `/本地测试/`、`/测试素材/`、`/实验/` 等规则，自己新建的测试文件夹不会被提交（`*.pdf`、`*.xlsx`、`*.docx`、`图片/` 本来就已经忽略） |
### v2.3.1（针对「原题区域 / 放大镜 / 配图加载失败」的加固）

| 项目 | 说明 |
|---|---|
| **扫描页取图提速** | 扫描件的原题区域改为**直接从已渲染的页面图裁切**（毫秒级），不再每次让 pdfplumber 重新栅格化整页 |
| **渲染串行化** | PDFium 不是线程安全的，现在所有渲染加锁串行；识别后的后台预热与前端并发取图不再互相干扰（实测冷启动并发取 16 张：3.0 秒、0 失败） |
| **两条路径互为兜底** | 任一种裁切方式失败会自动改用另一种；都失败时异常里会带上**具体原因** |
| **失败可见** | 核对页原题区域加载失败时会显示**真实的 HTTP 状态与错误内容**，并提供「重试」按钮（不再只显示一句「加载失败」） |
| **系统诊断** | 「⑥ 设置」新增**「系统诊断」**按钮：一键检查数据目录、缓存可写性、三个工作目录、PDF 是否可用，并做一次真实裁切测试，结果可复制 |
| **缓存定位统一** | 预热与读取共用同一个缓存目录（\`<程序目录>/.cache\`），不会再出现「预热白做」 |

> 以后再遇到加载失败：打开「⑥ 设置」→ 点「系统诊断」→ 把结果发出来即可精确定位。

### v2.3.2（目录归属修正）

| 项目 | 说明 |
|---|---|
| **输出/图片/缓存目录一律在 exe 所在目录** | 默认即 `<exe 所在目录>\错题集`、`\图片`、`.cache`。若 `settings.json` 里残留了失效路径（例如指向已删除的临时目录、或换机后不存在的盘符），启动时会**自动改回程序所在目录**并把目录建好 |
| **首次启动即建目录** | 第一次运行 exe 会自动创建 `错题集`、`图片`、`题目配置` 三个文件夹，以及默认 `settings.json` |
| **用户自选目录不会被误改** | 只有「空值 / 临时目录 / 父目录都不存在」才会回退默认；你自己新建的目录（父目录存在）会直接建出来并保留 |
| **配图显示更稳** | 有坐标的配图一律改为**从原卷现场裁剪**显示（配置文件里记录的图片路径即使已失效也不会再出现破图） |
